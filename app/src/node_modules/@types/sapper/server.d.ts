import { Request, Response, RequestHandler } from "express";
import { Session } from "./types";

export type MiddlewareOptions = {
  session?: (req: Request, res: Response) => Session
  ignore?: string | string[] | RegExp // TODO: add ignore function 
}

export const middleware: (opts?: MiddlewareOptions) => RequestHandler;

export type Middleware = typeof middleware;